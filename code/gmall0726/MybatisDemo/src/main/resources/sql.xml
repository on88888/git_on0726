<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    namespace:类似类的包名，目前可以随便写


-->
<mapper namespace="facai">
    <!--
        是什么语句，就用什么标签

        id: 语句的唯一标识，同一个namespace，语句的id必须唯一

        resultType： 语句查询的记录要封装的JavaBean的类型

        #{id}： #{xxxx} 固定语法，代表一个占位符，类似?

                xxxx该怎么写：
                        如果sql只有一个 ${}，xxxx随便写


       查询最后是如何封装数据的
    -->

    <select id="selectAnEmp" resultType="com.atguigu.mybatis.bean.Employee">

         select * from tbl_employee where id = #{ fjheoi;ahjfao;ifha }
  </select>



</mapper>